const AddressesTab = () => {
    // !@#$ we don't have ability to put multiple addresses into database so maybe we
    // can just put the user's current address in here?
    return (
        null
    )
}

export default AddressesTab;
